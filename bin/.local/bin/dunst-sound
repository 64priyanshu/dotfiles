#!/bin/bash

# Use the urgency to play different sounds
case "$DUNST_URGENCY" in
  LOW)
    paplay ~/.local/bin/.sounds/bell.wav
    ;;
  NORMAL)
    paplay ~/.local/bin/.sounds/message.wav
    ;;
  CRITICAL)
    paplay ~/.local/bin/.sounds/bell_end.wav
    ;;
esac
