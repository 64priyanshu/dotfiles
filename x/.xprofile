# For QT Themes
export QT_QPA_PLATFORMTHEME=qt5ct

# Environment variables set everywhere
export EDITOR="nvim"
export TERMINAL="kitty"
export BROWSER="firefox"

# Load .Xresources
xrdb -merge ~/.Xresources

# Touchpad/Mouse - xinput list
export TOUCHPAD=$(xinput list --name-only | grep -i touchpad)
# Trackpad speed (-1 to 1)
xinput set-prop "$TOUCHPAD" "libinput Accel Speed" 0

# Remap caps to escape
# setxkbmap -option caps:escape
# Swap escape and caps
# setxkbmap -option caps:swapescape

# Keys speed
xset r rate 550 25 # initial delay 600ms, repeat 25 ms

# Enable DPMS (Display Power Management Signaling)
xset +dpms

# Primary Display
export PRIMARY_DISPLAY=$(xrandr | awk '/ primary/{print $1}')

# Turn off screen after 3 minutes (180 seconds) and lock after 1 minute and Suspend system after 10 minutes (600 seconds) of inactivity
xidlehook \
  --not-when-fullscreen \
  --not-when-audio \
  --timer 180 \
  'xrandr --output "$PRIMARY_DISPLAY" --brightness .01' \
  'xrandr --output "$PRIMARY_DISPLAY" --brightness 1' \
  --timer 60 \
  'xrandr --output "$PRIMARY_DISPLAY" --brightness .01; i3lock -p default -i "$XDG_PICTURES_DIR/wallpapers/lock.png"' \
  'xrandr --output "$PRIMARY_DISPLAY" --brightness 1' \
  --timer 600 \
  'systemctl suspend' \
  '' &

# Screen Wallpaper
feh --bg-fill $XDG_PICTURES_DIR/wallpapers/wallpaper.png &
# feh --randomize --bg-fill $XDG_PICTURES_DIR/wallpapers/* &

# Network Manager Applet
nm-applet --indicator &

# Screenshots
flameshot &

# Notifications
dunst &

# Clipboard manager
greenclip daemon &
